# Copiar a .env y poner valores reales. .env está en .gitignore: NUNCA se sube al repositorio.
PSEUDO_SALT=poner-aqui-una-cadena-larga-y-secreta
WAREHOUSE_PATH=warehouse/red-metropolitana.duckdb
BRONZE_PATH=lake/bronze
DATOS_RED=datos_red
# kafka = Kafka real en Docker | simulado = sin Docker
STREAMING_MODO=kafka
KAFKA_BOOTSTRAP=localhost:9092
